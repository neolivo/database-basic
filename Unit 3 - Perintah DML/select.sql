-- select untuk menampilkan data
SELECT * FROM users;

-- untuk seleksi kolom dalam menampilkan
SELECT name, email FROM users;

-- untuk seleksi kondisonal dalam menampilkan
SELECT name, email FROM users WHERE email = 'davis@nusanio.com';
