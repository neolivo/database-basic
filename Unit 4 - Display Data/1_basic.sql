-- reminder untuk menampilkan semua data
-- pastikan bahwa data users ada 5 dan log_activity 10

-- memastikan data user
SELECT id, name, email FROM users;

-- memastikan data log_activity
SELECT id, user_id, activity FROM log_activity;